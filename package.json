{
  "name": "candid-neo",
  "version": "0.0.1",
  "description": "Candid — Miles apart. Frames together.",
  "copyright": "©️ Mewn",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix client & npm run dev --prefix server & wait",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "install": "npm install && npm install --prefix client && npm install --prefix server",
    "typecheck": "npm run typecheck --prefix client && npm run typecheck --prefix server",
    "lint": "npm run lint --prefix client && npm run lint --prefix server",
    "test": "npm run test --prefix client -- --run --passWithNoTests && npm run test --prefix server -- --run",
    "clean": "rm -rf node_modules client/node_modules server/node_modules client/dist server/dist"
  },
  "author": "©️ Mewn",
  "license": "UNLICENSED"
}